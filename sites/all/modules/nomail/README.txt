When enabled, this module does one thing: block Drupal from sending any email. Emails that are blocked by this module
are not queued; They are lost forever.

In ASU's context, it should be enabled when security scans are being run by the Internet Security Office (ISO).

